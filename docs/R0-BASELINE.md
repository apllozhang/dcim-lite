# dcim-lite — baseline

Clean-room Go implementation. Behavior targets a prior 0.1.x product where contract samples exist; source is original.

Evidence priority when implementing: live samples > DB constraints > OpenAPI > inferred models.

Record intentional behavior fixes in `COMPAT-DECISIONS.md`.
