# 评审批次 B：六屏人工交互验收矩阵（2026-09-13）

> 依据：《第六轮新旧UI复刻质量与主入口接替评审-20260913.md》§1.1 条件 4 与 §6 批次 B——
> "对六屏执行一次人工交互验收，覆盖按钮、对话框、筛选、分页、导入导出、返回和错误态；
> 不能只依据静态截图及成功路径 E2E"。
> 环境：`10.20.30.203:19502`（main@29bb51b 起），凭证 dev 栈 `admin/admin123456`；
> 写操作一律用 `MTX2-` 前缀创建临时资源并当场清理（与 e2e 的 `MTX-`/`DUAL-` 隔离）。
> 状态图例：✅=已有自动化证据　🔧=可自动化、待写授权排期补齐　👤=必须人工现场执行。

## 1. 通用项（六屏共有，AppLayout 层）

| 项 | 状态 | 证据/说明 |
|---|---|---|
| 应用壳结构对齐 v2 | 👤 | PR #58：logo 占左上角与顶栏 60px 齐平、顶栏自侧栏右缘起（紫线只跨右段）；对照截图随 PR 附 |
| 菜单导航/权限菜单 | ✅ | matrix 三权限矩阵（admin 有系统管理、user 无 + /admin 守卫重定向） |
| 浏览器返回 | 🔧 | 各屏返回按钮已有；浏览器级返回恢复待人工（自动化的 history 行为已由路由守卫单测覆盖） |
| 刷新恢复 | 👤 | F5 后停留当前路由、会话保持、数据重载 |
| 401 过期全局收口 | ✅ | 单测+e2e（token 清除、守卫弹回 /login） |
| 错误边界/接口 5xx 文案 | 👤 | 人工断后端或改代理触发（避免污染测试库） |

## 2. 屏 1 运行概览（/）

| 项 | 状态 | 证据/说明 |
|---|---|---|
| 加载/统计卡口径 | ✅ | e2e dashboard 文本断言（四统计卡/开发进度/服务在线） |
| 空态/筛选/分页/CRUD | — | 页面无此类交互 |
| 跳转入口 | ✅ | 三链接路由存在（e2e shell 断言） |

## 3. 屏 2 资源层级（/data-centers）

| 项 | 状态 | 证据/说明 |
|---|---|---|
| 加载/三栏级联选中 | ✅ | e2e（MTX-DC 出现、第二 DC 空机房空态提示） |
| 创建 DC/机房/机柜 | 🔧 | 表单打开/校验已由单测覆盖；真实提交待补自动化（写授权） |
| 编辑/复制/移动 | 🔧 | 对话框渲染单测覆盖；写链路待补 |
| 删除取消/删除失败 | 👤 | 取消路径人工点一遍；失败路径（含设备机柜拒绝删除）需构造 |
| 分页 | — | 机柜表走后端分页，同屏 4 |
| 空态 | ✅ | e2e（切第二 DC 空机房） |

## 4. 屏 3 设备台账（/devices）

| 项 | 状态 | 证据/说明 |
|---|---|---|
| 加载/九列表格/三列恢复 | ✅ | e2e 表头+行断言；19502 现场抽查 20260913（截图存档） |
| 筛选/搜索差分 | ✅ | e2e 新旧双跑（生命周期筛选、MTX-DV2 搜索） |
| 分页 | 👤 | 人工翻页核对总数与统计卡（147 台现存） |
| 创建/编辑设备 | 🔧 | 表单校验单测覆盖；真实提交待补（MTX2- 前缀） |
| 删除取消/删除失败 | 👤 | 人工（含在位设备删除被后端拒绝路径） |
| 批量导入 | ✅(预览)/🔧(提交) | e2e 已验证模板下载/文件解析/校验预览/匹配；真实提交+结果回执待补一行 MTX2- 数据 |
| 导出设备信息 | ✅ | e2e Node 侧读文件：43 列、当前筛选口径；19502 现场抽查 |
| U 位视图 Tab | ✅ | fetchULayout 映射修复后单测+页面断言 |

## 5. 屏 4 机柜管理（/racks）

| 项 | 状态 | 证据/说明 |
|---|---|---|
| 加载/四统计卡/筛选/搜索 | ✅ | e2e（MTX- 两柜、状态中文、搜索收窄） |
| 创建机柜（自动/手工编码） | 🔧 | 对话框+编码冲突重试单测覆盖；真实提交待补 |
| 编辑/详情抽屉/U 位入口 | ✅ | e2e U 位跳转（/devices?tab=layout&rackId）；详情抽屉人工过一遍 |
| 删除取消/删除失败（柜内有设备） | 👤 | 人工构造 |
| 批量导入（CSV） | 🔧 | 校验逻辑同 v2 口径已实现；真实提交待补；格式差异=登记项（CSV 替代 xlsx） |
| 分页 | 👤 | 人工（90 柜存量） |

## 6. 屏 5 机柜模板（/rack-templates）

| 项 | 状态 | 证据/说明 |
|---|---|---|
| 加载/系统模板标记 | ✅ | e2e（标准 42U 模板+系统 tag） |
| 新增（初始版本参数）/编辑 | 🔧 | 表单渲染单测；真实提交待补 |
| 发布新版本→当前版本立即更新 | 👤 | 人工发布一版并核对当前版本列与旧机柜快照不变（评审 3.5 验收点） |
| 系统模板删除禁用 | ✅ | e2e 断言禁用态 |

## 7. 屏 6 机房大屏（/room-screen）

| 项 | 状态 | 证据/说明 |
|---|---|---|
| 加载/暗色/像素基准 | ✅ | e2e mtx-new-room-screen 基准（mask 时钟） |
| 三空态 | ✅ | e2e+单测（⌗/▥/◍） |
| 状态筛选 chips/图例筛选 | ✅ | e2e+单测 |
| 拖动换槽位（持久化坐标） | ✅ | e2e 早期覆盖拖拽 swap；19502 现场复核建议 👤 |
| 上架/迁移/下架 | ✅ | e2e（写链路经 MTX- 种子，teardown 清理） |
| 设备查看详情/编辑（第 7 轮新增） | ✅ | e2e 菜单四项+抽屉分组+编辑对话框；19502 现场抽查 20260913 |
| 保存后原地更新/高度冲突报错 | 👤 | 人工改高度触发后端占用拒绝路径 |
| 机柜图导出→回导校验 | ✅ | e2e 真后端闭环（0 错误 0 待确认，不 commit） |
| PDU 管理/供电连接 | ✅ | PR #54 实测闭环；现场复核 👤 |
| 容量分析"无法计算"口径 | ✅ | e2e 指标卡断言 |
| 返回/全屏/时钟 | ✅ | 基准 mask+导航断言 |

## 8. 执行建议

1. **人工过一遍的顺序**：通用项 → 屏3 分页/删除路径 → 屏4 删除失败 → 屏5 发布新版本 → 屏6 拖拽复核+高度冲突，全程对照 `:19501` 旧 UI 同屏操作；预计 60-90 分钟。
2. **🔧 项的自动化补齐**（写授权后一批做）：六屏 CRUD 真实提交走 `MTX2-` 前缀 + teardown 自清理，复用 matrix.spec 的 loginAs/api 基建；导入真实提交补一行数据闭环。
3. 验收中发现的问题按"契约差异/如实化登记/缺陷"三分类记录，缺陷标 P0-P2，回到标准闭环流程。
